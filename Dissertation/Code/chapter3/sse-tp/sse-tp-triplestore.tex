State:
    $storageEngine$ := createStorageEngine()

// $[E(node_0), ..., E(node_n)] \leftarrow \textrm{SSE-TP}.Search(triplestoreID, \; [trapdoor_0, ..., trapdoor_n])$ 
function search($triplestoreID$, $trapdoors$):
    return $storageEngine$.search($triplestoreID$, $trapdoors$)

// $\{uploadID, \bot\} \leftarrow \textrm{SSE-TP}.PrepareUpload(triplestoreID, \; E(nodes))$
function prepareUpload($triplestoreID$, $encryptedNodes$):
    return $storageEngine$.commitUpload($triplestoreID$, $encryptedNodes$)

// $\{deleteID, \bot\} \leftarrow \textrm{SSE-TP}.PrepareDelete(triplestoreID, \; [trapdoor_0, ..., trapdoor_n])$
function prepareDelete($triplestoreID$, $trapdoors$):
    return $storageEngine$.commitDelete($triplestoreID$, $trapdoors$)

// $\{\textrm{SUCCESS}, \bot\}$ $\leftarrow \textrm{SSE-TP}.Update$($triplestoreID$, $[updateID_0,...,updateID_n]$, $[deletionID_0, ..., deletionID_n]$) 
function update($triplestoreID$, $uploadIDs$, $deletionIDs$):
    return $storageEngine$.update($triplestoreID$, $uploadIDs$, $deletionIDs$)

// $\{\textrm{SUCCESS}, \bot\} \leftarrow \textrm{SSE-TP}.DeleteAll(triplestoreID)$:    
function deleteAll($triplestoreID$)
    return $storageEngine$.delete($triplestoreID$)