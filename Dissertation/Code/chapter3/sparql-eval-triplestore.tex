State:
    $storageEngine$ := createStorageEngine()
    
# SPARQL-EVAL.evaluateQuery($triplestoreID$, $plan$):
function evaluateQuery($triplestoreID$, $plan$):
    $worker$ := createQueryWorker()
    $worker$.setContext($triplestoreID$, $storageEngine$)
    $execution$ := createQueryExecution()
    $execution$.exec($plan$, $worker$)
    return $execution$.getSPARQLResult()

function saveTriples($triplestoreID$, $triples$):
    return $storageEngine$.save($triplestoreID$, $triples$)

function deleteTriples($triplestoreID$, $triples$):
    return $storageEngine$.delete($triplestoreID$, $triples$)

function delete($triplestoreID$):
    return $storageEngine$.delete($triplestoreID$)