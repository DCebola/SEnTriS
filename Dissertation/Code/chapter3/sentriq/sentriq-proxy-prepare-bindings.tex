State:
    $storageEngine$ := createStorageEngine()

// $\{ searchID, \; \bot \} \leftarrow \textrm{SEnTriQ}.PrepareBindings([E(binding_0),...,E(binding_n)])$
function prepareBindings($bindings$):
    return $storageEngine$.save($bindings$) 