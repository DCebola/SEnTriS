function createTriplestore($cookie$, $triplestoreID$, $issuer$):
    if $cookie$ = :bot
        return :bot
    $result$ := IAM_Provider.createTriplestoreAccessList($cookie$, $triplestoreID$, $issuer$)
    if $response$ = :bot: 
        IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
        return :bot
    $scheme$ := createEncryptionScheme() 
    $response$ := Vault.saveSecrets($triplestoreID$, $scheme$.getSecrets(), $accessToken$)
    if $response$ = :bot:
        $reponse_2$ := IAM_Provider.deleteTriplestoreAccessList($cookie$, $triplestoreID$, $issuer$, $accessToken$)
        if $reponse_2$ = :bot
            IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
        return :bot
    IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
    return $response$

function deleteTriplestore($cookie$, $triplestoreID$, $issuer$):
    if $cookie$ = :bot
        return :bot
    $accessToken$ := IAM_Provider.createAccessToken($cookie$, $issuer$, $triplestoreID$)
    if $accessToken$ = :bot:
        return :bot
    $response$ := IAM_Provider.acquireTriplestoreLock($cookie$, $triplestoreID$, $accessToken$)
    if $response$ = :bot: 
        IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
        return :bot
    $reponse$ := Vault.deleteSecrets($triplestoreID$, $accessToken$)
    if $response$ = :bot: 
        IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
        return :bot
    $reponse$ := IAM_Provider.deleteTriplestoreAccessList($cookie$, $triplestoreID$, $issuer$, $accessToken$)
    if $response$ = :bot: 
        IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
        return :bot
    return $response$

function listTriplestores($cookie$, $issuer$, $write$, $read$, $owns$):
    if $cookie$ = :bot
        return :bot
    return IAM_Provider.listTriplestores($cookie$, $issuer$, $write$, $read$, $owns$)

function updateTriplestoreOwner($cookie$, $triplestoreID$, $issuer$, $target$):
    if $cookie$ = :bot
        return :bot
    $accessToken$ := IAM_Provider.createAccessToken($cookie$, $issuer$, $triplestoreID$)
    if $accessToken$ = :bot:
        return :bot
    $response$ := IAM_Provider.acquireTriplestoreLock($cookie$, $triplestoreID$, $accessToken$)
    if $response$ = :bot: 
        IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
        return :bot
    $response$ := IAM_Provider.updateTriplestoreOwner($cookie$, $triplestoreID$, $target$, $accessToken$)
    IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
    return $response$

function issueAccessRequest($cookie$, $triplestoreID$, $issuer$, $write$):
    if $cookie$ = :bot
        return :bot
    return IAM_Provider.requestAccess($cookie$, $triplestoreID$, $issuer$, $write$)

function listPendingAccessRequests($cookie$, $triplestoreID$, $issuer$):
    if $cookie$ = :bot
        return :bot
    $accessToken$ := IAM_Provider.createAccessToken($cookie$, $issuer$, $triplestoreID$)
    if $accessToken$ = :bot:
        return :bot
    $response$ := IAM_Provider.listPendingAccessRequests($cookie$, $triplestoreID$, $accessToken$)
    IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
    return $response$

function processPendingAccessRequest($cookie$, $triplestoreID$, $issuer$, $requestID$, $accept$):
    if $cookie$ = :bot
        return :bot
    $accessToken$ := IAM_Provider.createAccessToken($cookie$, $issuer$, $triplestoreID$)
    if $accessToken$ = :bot:
        return :bot
    $response$ := IAM_Provider.processAccessRequest($cookie$, $triplestoreID$, $requestID$, $accept$, $accessToken$)
    IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
    return $response$

function grantAccess($cookie$, $triplestoreID$, $issuer$, $target$, $write$):
    if $cookie$ = :bot
        return :bot
    $accessToken$ := IAM_Provider.createAccessToken($cookie$, $issuer$, $triplestoreID$)
    if $accessToken$ = :bot:
        return :bot
    $response$ := IAM_Provider.acquireTriplestoreLock($cookie$, $triplestoreID$, $accessToken$)
    if $response$ = :bot: 
        IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
        return :bot
    $response$ := IAM_Provider.grantAccess($cookie$, $triplestoreID$, $target$, $write$, $accessToken$)
    IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
    return $response$

function revokeAccess($cookie$, $triplestoreID$, $issuer$, $target$, $write$):
    if $cookie$ = :bot
        return :bot
    $accessToken$ := IAM_Provider.createAccessToken($cookie$, $issuer$, $triplestoreID$)
    if $accessToken$ = :bot:
        return :bot
    $response$ = acquireTriplestoreLock($cookie$, $triplestoreID$, $accessToken$)
    if $response$ = :bot: 
        IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
        return :bot
    $response$ := IAM_Provider.revokeAccess($cookie$, $triplestoreID$, $target$, $write$, $accessToken$)
    IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
    return $response$

function listUsersWithAccess($cookie$, $triplestoreID$, $issuer$, $write$):
    if $cookie$ = :bot
        return :bot
    $accessToken$ := IAM_Provider.createAccessToken($cookie$, $issuer$, $triplestoreID$)
    if $accessToken$ = :bot:
        return :bot
    $response$ := IAM_Provider.listUsersWithAccess($cookie$, $triplestoreID$, $write$, $accessToken$)
    IAM_Provider.deleteAccessToken($cookie$, $triplestoreID$, $accessToken$)
    return $response$
    
