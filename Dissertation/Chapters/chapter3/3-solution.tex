%%!TEX root = ../../template.tex
\typeout{NT FILE chapter3}

\chapter{SEnTri: A Searchable Encrypted Triplestore}
\label{cha:solution}

\prependtographicspath{{Figures/}}

In this chapter we will present the main contribution of our research. In Section \ref{ch3:sec:solution-models}, we introduce \gls{SEnTri} and define the system and adversary model. In Section \ref{ch3:sec:solution-definition} we formally define the proposed solution, specify its enforcement of security guaranties and present the \gls{SPARQL} query evaluation pipeline: triple pattern encryption schemes,  high level execution pipelines and protocols, reasoning preservation techniques and list the supported query types.

\section{Solution Models}
\label{ch3:sec:solution-models}

\subsection{Overview}

The current \emph{state-of-the-art} solutions solely focus on the problem of secure \gls{RDF} data outsourcing, leaving the problem of dynamic updates out of scope. Furthermore, these systems do not have reasoning capabilities, suffer from leakage problems and, frequently, solely provide search over simple \emph{triple patterns}.

Considering all these limitations we developed \gls{SEnTri}, a solution for secure and cooperative development of semantic knowledge bases.

\gls{SEnTri} possesses role-based \emph{access control} and can execute secure read and write operations. \mytodo{Do not forget to mention verification of authenticity and data integrity as future work} It supports most \gls{SPARQL} operations and retains reasoning capabilities while evaluating queries.

\gls{SEnTri} is designed for a multi-user setting. It is purely a software solution, not requiring any special hardware. It is also \emph{transparent} (working with encrypted ontologies does not affect a user's workflow in any significant way) and outsources storage and heavy computations to external servers. Furthermore, it remains \emph{flexible} and is fully \emph{modular}.


\subsection{System Model}
\label{ch3:sec:solution-models:system-model}

\paragraph{Overview} In \gls{SEnTri}, authenticated \emph{users} are able to create \emph{encrypted triplestores}. Within a triplestore they are able to upload, modify and query semantic data. Triplestores can be shared between users with varying degrees of access.

\gls{SEnTri} follows the usual \gls{SE} schemes architecture. It is a \emph{client/server} application which outsources data storage and heavy computations to third parties. It follows the \gls{M/M} model with \emph{key sharing} between trusted users. Figure \mytodo{Add system model figure} depicts a high level overview of the system.

\paragraph{System Architecture} \gls{SEnTri} is a multi-party system comprised of 5 components:

\begin{itemize}
    \item \emph{Client}: a public facing component where users can authenticate themselves and create, upload, share, request or revoke access to and query triplestores.
    \item \emph{Triplestore}: a storage component where all encrypted data is saved;
    \item \emph{Secure Computation Proxy}: this component executes secure query plans and set operations between encrypted \emph{bindings};
    \item \emph{\gls{IAM} Provider}: a component that handles authentication and validates access policies.
    \item \emph{Vault}: a component to store confidential data required by the cryptographic protocols (e.g. cryptographic keys);
\end{itemize}

Each individual component is a web server that exposes a \glsxtrshort{REST} \gls{API}. All communication channels between components are secure and reliable. They follow the \glsxtrshort{HTTPS} \autocite{noauthor_undated-yf} protocol and \glsxtrshort{TLS} \autocite{noauthor_undated-kt} data encryption with mutual authentication.  Figure \mytodo{Add system architecture figure} depicts a high level architecture of the solution.

\paragraph{Role-Based Access Control} In \emph{SEnTri} roles grant different permissions to the users, which restrict the type of operations they can execute within the system. Users can have the \emph{ADMIN}, \emph{PRIVILEGED} and \emph{BASIC} roles. For ease of communication from now onwards, we will refer to a user with each role, respectively, as \emph{admin}, \emph{privileged} and \emph{basic} user.

\emph{Basic} users can only query existing triplestores, if they have been given access to do so. They can request \emph{read} or \emph{write} access to existing triplestores.

\emph{Privileged} users have all the permissions of a \emph{basic} user. Additionally, they are able to create new triplestores, grant or revoke read/write access to their triplestores, transfer ownership to other users and process access requests targeting triplestores they own.

\emph{Admin} users have all the permissions of a \emph{privileged} user but can also manage users, any triplestore and process role change requests.


\paragraph{M/M setting} In \gls{SEnTri} access to triplestores is enforced through the use of \emph{ephemeral access tokens}. These tokens are only obtainable by authenticated users that belong to the respective triplestore's read and write access whitelist.

These tokens ensure that the sharing of \emph{secrets}, needed for the cryptographic protocols, and that the write/read operations are only done by users that have the required permissions to do so.

Correctness of concurrent write and read operations is achieved with a \emph{lock-based} approach when issuing access tokens. \emph{Read access tokens} can be issued at anytime, but only one \emph{write access token}, per triplestore, can exist at any given moment. \mytodo{Add example of access token emission \& validation (\gls{IAM} Provider, Vault, Triplestore)}

\subsection{Security Model}
\label{ch3:sec:solution-models:security-model}

\paragraph{Assumptions} We will consider an \emph{honest-but-curious} adversary. This adversary will try to eavesdrop communication between servers and, if it has access to the external servers where the semantic data and execution logs are stored, try to infer or decrypt sensitive information.

Following the architecture of \gls{SEnTri}, this means that the \emph{Triplestore} and \emph{Proxy} components will be the targets of attacks. The other components and communication channels are trusted, reliable and secure. We will also assume that authenticated users are trusted and non-malicious. Such users can read sensitive information and security parameters of the triplestores they have created or have been given access to.

When targeting these components, the attackers will try to decrypt data and infer sensitive information from the leakage of the protocols in use.

Additionally, we will not consider the case where attacks involve \emph{revoked users}. \footnote{The role-based access controls prevent these users from interacting (via client) with triplestores they do not have permissions to. Nevertheless, we have not implemented a key rotation, or re-encryption protocol for data. As such, an adversary collaborating with these users, or an attack from such users, will always succeed in the decryption the data of all the triplestores they used to have access to. They can still possess the cryptographic secrets used by the protocols, which makes it trivial to decrypt leaked information.}



\paragraph{Security Properties} In conformity with these assumptions we expect \gls{SEnTri} to possess the following \emph{security properties}:

\begin{enumerate}
    \item An external observer will not be able to learn any useful information from eavesdropping messages sent between components.
    \item An attacker will not be able to impersonate any component.
    \item An attacker will not be able to tamper with messages sent between components.
    \item An attacker with access to the \emph{Triplestore} component will learn nothing more than the size of a triplestore and its \emph{search} and \emph{access} patterns.
    \item Given two arbitrary queries $q_1$, $q_2$ and two encrypted query plans $p_A$, $p_B$ sent to the \emph{Proxy}. An attacker with access to this component, will not be able to distinguish which query plan corresponds to a given query with a probability higher than that of random chance.
    \item Regarding \emph{term equality} during intermediate set operations between \emph{bindings}, an attacker with access to the \emph{Proxy} component will learn nothing more than:
          \begin{enumerate}
              \item \emph{First Protocol}: the equality of all terms, belonging to a series of queries, targeting the same triplestore.
              \item \emph{Second Protocol} the equality of all terms present in the graph patterns of individual queries. The bindings of different queries are indistinguishable.
          \end{enumerate}
\end{enumerate}

\pagebreak

\section{Solution Definition}
\label{ch3:sec:solution-definition}

\subsection{Overview}

The functioning of a query evaluation system can be described as a pipeline of sequential operations. Firstly, if the system supports authentication, there is an authentication step. An authenticated user will be able to submit a series of queries while their session is active. A submitted query is parsed, and given the specified heuristics, the system generates a series of query execution plans, sequences of logical operations (e.g. data retrieval, update or execution of an equijoin). After a query plan is chosen and executed, the final results are sent to the user, with the applied solution modifiers. The following figure depicts this pipeline \mytodo{Add figure with standard query evaluation pipeline}.

\emph{SEnTri} follows a similar logic for query evaluation with a few modifications to enhance security. The system only works with encrypted data, which is encrypted in a manner that allows for search and set operations to be executed. Additionally, before each step of the pipeline, access checks are performed against the users' ephemeral access tokens, to ensure that only the right actors are performing actions within the system. Figure shows the modified pipeline \mytodo{Add figure with SEnTri query evaluation pipeline}.

In \gls{SEnTri}, data encryption retains elements from the \emph{CryptDB} approach \autocite{Popa2011-zb, Popa2012-bw, Popa2011-ci} employing encryption onions, which are peeled when certain data properties are needed, namely \emph{equality} for set operations between \emph{bindings}. It also incorporates techniques from \emph{SSE} encryption schemes: we generate trapdoors for secure search and retrieval of encryption onions, which hold encrypted subjects, predicates and objects present in \emph{triple patterns}. The searchable encrypted onions are essential for the dynamic creation of \emph{binding tables} which are then used for the evaluation of complex \emph{graph patterns}.

We have implemented \gls{SEnTri} using two different approaches. Both rely on encrypted onions with two layers of encryption, where the outer layer is probabilistically encrypted.

In the first approach, the inner layer is deterministically encrypted, achieving equality like \emph{CryptDB}, whereas in the second approach, the inner layer is homomorphically encrypted, using an algorithm that preserves additive properties. In the rest of the document we will refer to the two approaches, respectively, as V1 and V2.


\mytodo{Add figure with two implementation overview scheme... interaction of components like for a poster}

\subsection{Preliminaries}

Prior to formally defining the solution, we will specify the cryptographic tools and core elements of \gls{SEnTri}.

\subsubsection{Cryptographic Tools}

\paragraph{Notation} The function $E(K, \; m, \; iv)$ denotes the encryption of message $m$, using the initialization vector $iv$ and key $K$. The function $D(K, \; ct)$ denotes the decryption of ciphertext $ct$, using key $K$. The keys used as input will identity the encryption scheme (e.g. $K_{A}$ implies the usage of the symmetric encryption scheme $A$ and $K_{B_{priv}}$ implies the usage of public key encryption scheme $B$).

\paragraph{Encryption Schemes} $RND$ and $DET$ will denote, respectively, a non-deterministic and deterministic encryption schemes. $DGK$ will identify the \gls{DGK} cryptosystem, an additive homomorphic encryption scheme with interesting properties that can be leveraged to execute secure integer comparison \autocite{Damgard2007-km, Damgard2009-my}. The $RND$ and $DET$ are used in protocol V1 and \gls{DGK} is used in protocol V2. We will now provide a definition for the  \gls{DGK} scheme \autocite{Franz2011-sm, Makkes2010-qb}:

\begin{definition}[\textbf{DGK Encryption}]\label{ch3:definition:dgk} Let $k$, $t$, and $l$ be public parameters. Parameter $k$ determines the size of a \gls{RSA} modulus, $l$ is the bit length of the messages to encrypt and $t$ should be large enough so that exhaustive search is infeasible.

\paragraph{Key Generation} Let $p$, $q$ be two primes, generated such that $n = pq$ is a $k$-bit \gls{RSA} modulus and that the $u$, $v_p$ and $v_q$ also exist and possess the following properties:

\begin{changemargin}{1cm}{1cm}
    \begin{enumerate}
        \item $u$ is the smallest prime greater than $l + 2$
        \item $u$ is a divisor of both $p - 1$ and $q -1$
        \item $v_p$ is $t$-bit prime numbers that divides $p - 1$
        \item $v_q$ is $t$-bit prime numbers that divides $q - 1$
    \end{enumerate}
\end{changemargin}


Choose random values $g, h \in \mathbb{Z}^*_n$, so that:

\begin{changemargin}{1cm}{1cm}
    \begin{enumerate}
    \item the multiplicative order of $h$ is $v_pv_q$
    \item $g$ is an element of order $uv_pv_q \in \mathbb{Z}^*_n$
    \end{enumerate}
\end{changemargin}

The message space is $M = \mathbb{Z}_u$ and the ciphertext space is $C = \{g^mh^r \; \textrm{mod} \; n \; | \; m \in \mathbb{Z}_u, \;  r \in \mathbb{Z}_{v_pv_q}\} \subseteq \mathbb{Z}^*_n$. The public and private keys are, respectively, $K_{DGK_{pub}} = (n,\;g,\;h,\;u)$ and $K_{DGK_{priv}} = (p,\;q,\;v_p,\;v_q)$.

\paragraph{Probabilistic Encryption} An encryption $ct$ of $m$ is calculated as:
\begin{equation}\label{chapter3:definition:dgk:equation:rnd-enc}
    ct = g^mh^r  \; \textrm{mod} \; n
\end{equation}

The value $r \in \mathbb{Z}_{v_pv_q}$ is only possible to calculate if we have access to private key. Knowing the public key, we instead determine the value of $r$ as a random $2.5t$-bit integer, so that $h^r$ is indistinguishable from a uniformly random element from the subgroup generated by $h$.

\paragraph{Deterministic Encryption} To compute a deterministic encryption of $m$ we remove the randomization part, $h^r \; \textrm{mod} \; n$, from the calculations:
\begin{equation}\label{chapter3:definition:dgk:equation:det-enc}
    ct = g^m \; \textrm{mod} \; n
\end{equation}

\paragraph{Re-encryption} To re-encrypt a probabilistic encryption of $m$ we sample a new random $2.5t$-bit integer $r_2$ and compute:
\begin{align}\label{chapter3:definition:dgk:equation:re-enc}
    \begin{split} 
    ct_2 &= ct \cdot h^{r_2} \; \textrm{mod} \; n \\
    &= g^mh^rh^{r_2} \; \textrm{mod} \; n \\
    &= g^mh^{r + r_2} \; \textrm{mod} \; n \\
    \end{split}
\end{align}

\paragraph{Decryption} The decryption of a ciphertext $ct$ is done by calculating:

\begin{align}
    \begin{split}
    c^{v_pv_q} \; \textrm{mod} \; n &= (g^mh^r)^{v_pv_q} \; \textrm{mod} \; n \\
        &= (g^m)^{v_pv_q}(h^r)^{v_pv_q} \; \textrm{mod} \; n \\ 
        &= (g^m)^{v_pv_q}*1 \; \textrm{mod} \; n \;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\; \text{because order of h is $v_pv_q$}\\
        &= (g^{v_pv_q})^m \; \textrm{mod} \; n
    \end{split}
\end{align} 

Due to the value of $u$ being small, it is feasible to search for the calculated value in a look-up table with tuples of type $((g^{v_pv_q})^i \; \textrm{mod} \; n, \; i)$, for $0 < i < u$. Even without the look-up table, one can quickly determine if $ct$ is an encryption of 0, because $D(0) = 1$:

\begin{equation}\label{chapter3:definition:dgk:equation:zero-dec}
    c^{v_pv_q} \; \textrm{mod} \; n = (g^{v_pv_q})^m \; \textrm{mod} \; n    \stackrel{\text{m = 0}}{=} (g^{v_pv_q})^0 \; \textrm{mod} \; n = 1 \; \textrm{mod} \; n = 1
\end{equation}

\paragraph{Homomorphic property} The \gls{DGK} cryptosystem is homomorphic with respect to the addition operation ($+$). For $ct_1, ct_2 \in C$, there exists $m_1, m_2 \in M$ and $r_1, r_2 \in \mathbb{Z}_{v_pv_q}$, where $ct_1 = g^{m_1}h^{r_1}  \; \textrm{mod} \; n$ and $ct_2 = g^{m_2}h^{r_2}  \; \textrm{mod} \; n$ and equation \ref{ch2:sec:homomorphic-encryptio:def-eq} holds:

\begin{equation}
    \begin{split}
    E(m_1) * E(m_2) &= (g^{m_1}h^{r_1} \; \textrm{mod} \; n) * (g^{m_2}h^{r_2} \; \textrm{mod} \; n) \\ 
        &= ( g^{m_1}h^{r_1}  \; * g^{m_2}h^{r_2} ) \; \textrm{mod} \; n \\ 
        &= g^{(m_1 + m_2)}h^{(r_1 + r_2)} \; \textrm{mod} \; n \\ 
        &= E(m_1 + m_2)
    \end{split}
\end{equation}

\end{definition}

\subsubsection{Core elements}

\paragraph{Triplestores} A \gls{RDF} dataset within \gls{SEnTri} is represented as a single named triplestore. A triplestore $T = (triplestore_{ID}, \;  nodes, \; acp)$ is a key value store, where $triplestore_{ID}$ is a unique string that identifies the dataset, $nodes = \{(idx_{0}, node_{0}), ... ,(idx_{n},\allowbreak node_{n})\}$ is a map, $node_i \in I \cup B \cup L$ is uniquely identifiable by $idx_i$, $idx_i$ is a hash of the \emph{triple pattern} that $node_i$ belongs to, and $acp$ is the triplestore's \gls{ACP}.

This data structure is a simplified version of the one defined in section \ref{ch2:sec:semantic-web:sparql}, where we only represent the \emph{default} graph.

\paragraph{Triplestore Access Control Policy} A triplestore's \gls{ACP} $acp = (r,\; w,\; acrl)$, is a tuple where $r$ and $w$ are both collections of usernames and $acrl$ is a collection of \emph{access change requests}. An access change request $access_{req} = (request_{ID}, \; username, \; write)$ is a tuple where $request_{ID}$ is a freshly generated \gls{UUID} (version 4) \autocite{Leach2005-rf}, $username$ identifies the user that this request affects and $write$ is a boolean, that specifies if the user desires write access (true) or read access (false).

\paragraph{Users} A user $u = (username, \; password, \; owned, \; role)$ is a tuple, where $username$ and $password$ are unique strings, $owned = [triplestore_{ID_0},...,triplestore_{ID_n}]$ is a collection of user's owned triplestores identifiers and $role \in \{\textrm{BASIC}, \; \textrm{PRIVILEGED},\; \textrm{ADMIN}\}$.


\paragraph{Role Change Request} A role change request $role_{req} = (request_{ID}, username, role)$ is a tuple where $request_{ID}$ is a freshly generated \gls{UUID} (version 4) \autocite{Leach2005-rf}, $username$ identifies the user that this request affects and $role \in \{\textrm{BASIC}, \; \textrm{PRIVILEGED}, \textrm{ADMIN}\}$ specifies the desired new role.


\paragraph{Sessions} A user's session $s = (session_{ID},\; username,\; ttl)$ is a tuple, where  $session_{ID}$ is a freshly generated \gls{UUID} (version 4) \autocite{Leach2005-rf}, $username$ belongs to a valid registered $user$ and $ttl \in N$ specifies the maximum time (in seconds) that a session can remain active, before expiring. 


\paragraph{Locks} A lock is $l = (lock_{ID},\; resource_{ID},\; ttl)$ is a tuple, where  $lock_{ID}$ is a freshly generated \gls{UUID} (version 4) \autocite{Leach2005-rf}, $resource_{ID}$ is either a valid $username$ or $triplestore_{ID}$ and $ttl \in N$ specifies the maximum time (in seconds) that a lock can remain valid, before expiring.


\paragraph{Ephemeral Access tokens} An ephemeral access token $at = (token_{ID}, \; ttl, \; username, \allowbreak session_{ID}, \; triplestore_{ID}, \; lock_{ID})$ is a tuple, where $token_{ID}$ is a freshly generated \gls{UUID} (version 4) \autocite{Leach2005-rf}, $ttl \in N$ specifies the remaining time (in seconds) that the token will stay active, $username$ is the identifier of the user who holds the token, $session_{ID}$ is the identifier of the token holder's active session, at the time of its creation, $triplestore_{ID}$ is the identifier of the triplestore whose access is being requested and $lock_{ID}$ can either be $\bot$ (for \emph{read} access requests) or the identifier of the current lock, against the targeted triplestore (for \emph{write} access requests).


\paragraph{SPARQL queries} The definition of a \gls{SPARQL} query within \gls{SEnTri} is equal to that provided in section \ref{ch2:sec:semantic-web:sparql}.

\paragraph{Secret Maps} A secret map $smap = (triplestore_{ID}, \; secrets)$ is a tuple where $triplestore_{ID}$ is the identifier of an existing triplestore, $secrets = \{(key_0, \; secret_0), ..., \allowbreak (key_n, \; secret_n)\}$ is a map and $key_i$ is a unique string that identifies $secret_i$, the serialized value of a secret parameter.


\paragraph{Query Execution Plans} A query execution plan is a tuple $plan=(jobs, \;execOrder)$, where $jobs = \{(job_{ID_0}, \;, job_0),...,(job_{ID_n}, \;, job_n)\}$ is a map that maps query $job$, an operation that is executed against the intermediate \gls{SPARQL} query's \emph{bindings}, to their respective identifiers, freshly generated \gls{UUID} (version 4) \autocite{Leach2005-rf}, and $execOrder$ is a sequence of job identifiers.

\paragraph{Query Jobs} Query jobs can be of three type: \emph{basic}, \emph{unary} and \emph{binary} jobs. Unary and binary jobs execute against the results of, respectively, one and two other jobs, whereas basic jobs are independent of other jobs' results.

There exist two basic jobs, the search operation $job_{SEARCH}$ and the operation that returns empty \emph{bindings}:
\begin{itemize}
    \item \emph{Search Job}: a tuple $job_{SEARCH} = (job_{ID}, \; vars, \; searches)$, where $vars = [var_0, ..., var_n]$ is a collection of variables and $searches = \{(var_0, \; search_{ID_0}), ...,  (var_n, \; search_{ID_n})\}$ is a map that maps variables to the identifiers of search results. These identifiers $search_{ID}$ are freshly generated \gls{UUID}s (version 4) \autocite{Leach2005-rf}, created after the successful upload of search results to the \emph{Proxy} component.
    \item \emph{Empty Result Job}: a tuple $job_{EMPTY} = (job_{ID}, \; vars)$, where $vars = [var_0, ..., var_n]$ is a collection of variables.
\end{itemize}

There exist four unary jobs, the operations that apply result modifiers. These consist in:

\begin{itemize}
    \item \emph{Project Job}:
    \item \emph{OrderBy Job}:
    \item \emph{Slice Job}:
    \item \emph{Distinct Job}:
\end{itemize}

Finally, there are four binary jobs, which represent set operations between \emph{binding} tables:

\begin{itemize}
    \item \emph{Join Job}:
    \item \emph{Union Job}:
    \item \emph{Optional Job}:
    \item \emph{Minus Job}:
\end{itemize}

\paragraph{\emph{Binding} tables}


\subsection{Formal definition of SEnTri}
\label{ch3:sec:solution-definition:formal-definition}

\begin{definition}[\textbf{SEnTri}]\label{ch3:definition:sentri}
\gls{SEnTri} is a collection of 5 protocols $(\textrm{IAM}, \; \textrm{CLIENT}, \; \textrm{VAULT}, \allowbreak \textrm{TRIPLESTORE}, \; \textrm{PROXY})$ where:

\begin{itemize}
    \item IAM is a tuple $(auth, \; registerUser, \; deleteUser, \; issueGrantRoleRequest, \; getPend{\text-}\linebreak ingRoleRequests, \; processRoleRequest, \; createTriplestoreAccessPolicy, \;listTriple{\text-}\linebreak stores, \; listUsersWithAccess, \; changeTriplestoreOwner, \; deleteTriplestoreAccess{\text-}\linebreak Policy, \; grantAccess, \; revokeAccess, \; issueAccessRequest, \; getPendingAccessRequests,\linebreak processAccessRequest, \; createAccessToken, \; deleteAccessToken, \; checkReadAccess, \linebreak checkWriteAccess, \; checkOwnerAccess, \; checkIfActive, \; acquireTriplestoreLock, \linebreak releaseTriplestoreLock)$ of 24 polynomial time algorithms. These algorithms are responsible for session validation, user data and role management, triplestore access policies management and granting or revoking access to triplestores:
    \begin{itemize}
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{auth(username, \; password)}$: takes as input a pair of user credentials. Outputs $\textrm{SUCCESS}$ if a new session was generated, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{registerUser(username, \; password)}$: takes as input a pair of user credentials. Outputs $\textrm{SUCCESS}$ if a new user is registered, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{deleteUser(cookie, \; username)}$ takes as input a cookie and the username of the user to delete. Outputs $\textrm{SUCCESS}$ if the user is successfully deleted, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow  \mathbf{issueGrantRoleRequest(cookie, \; username, \;issuer, \; role)}$: \linebreak takes as input a cookie, the usernames of the target and issuer of the role changing request and the desired role. Outputs $\textrm{SUCCESS}$ if the request is successfully persisted, $\bot$ otherwise.
        
        \item $[role_{req_0},...,role_{req_n}] \leftarrow  \mathbf{getPendingRoleRequests(cookie, \; username)}$: takes as input a cookie, and the request issuer username. Outputs the collection of pending role changing requests.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{processRoleRequest(cookie, \; username, \; request_{ID}, \; target,}$ \linebreak $\mathbf{decision)}$: takes as input a cookie, the username of the processing request's issuer, the request identifier, the username of the role changing target and the issuer's decision. Outputs $\textrm{SUCCESS}$ if the role changing request is successfully processed, according to the provided decision, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{createTriplestoreAccessPolicy(cookie, \; issuer, \; triplesto{\text -}}$\linebreak $\mathbf{re_{ID})}$: takes as input a cookie, the username of the request's issuer and a triplestore identifier. Outputs $\textrm{SUCCESS}$ if the triplestore access policy is sucessfully generated, $\bot$ otherwise.
        
        \item $[triplestore_{ID_0}, ..., triplestore_{ID_n}]  \leftarrow \mathbf{listTriplestores(cookie, \; issuer, \; owns, }$\linebreak$\mathbf{read, \; write)}$: takes as input a cookie, the username of the request's issuer and three boolean filter parameters. Outputs a collection of triplestore identifiers: if $owns$ is \emph{true}, returns identifiers of \emph{owned} triplestores; if $read$ is \emph{true}, returns identifiers of triplestores that the user has \emph{read} access; if $write$ is \emph{true}, returns identifiers of triplestores that the user has \emph{write} access; 
        
        \item $\{msg, \bot\} \leftarrow \mathbf{listUsersWithAccess(cookie, triplestore_{ID}, \; write, \; at)}$: takes as input a cookie, a triplestore identifier, a boolean filter parameter and an access token. If the access token has sufficient permissions, outputs a message with the specified triplestore's \emph{owner} username and the collection of \emph{read} and \emph{write} access (if the $write$ parameter is \emph{true}) users' usernames, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{changeTriplestoreOwner(cookie, \; triplestore_{ID}, \; target, \; at)}$: takes as input a cookie, a triplestore identifier, a username and an access token. Outputs $\textrm{SUCCESS}$ if the ownership of the specified triplestore is successfully transfered to the $target$ user, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{deleteTriplestoreAccessPolicy(cookie, \; triplestore_{ID}, \; at)}$: \linebreak takes as input a cookie a triplestore identifier and an access token. Outputs $\textrm{SUCCESS}$ if the specified triplestore access policy is successfully deleted, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{grantAccess(cookie, \; triplestore_{ID}, \; target,\; write, \; at)}$: takes as input a cookie, a triplestore identifier, a username, a boolean parameter and an access token. Outputs $\textrm{SUCCESS}$ if the $target$ user is successfully granted \emph{read}, or \emph{write} (if $write$ parameter is \emph{true}), access against the specified triplestore, $\bot$ otherwise. 
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{revokeAccess(cookie, \; triplestore_{ID}, \; target,\; write, \; at)}$: \linebreak takes as input a cookie, a triplestore identifier, a username, a boolean parameter and an access token. Outputs $\textrm{SUCCESS}$ if the $target$ user \emph{read}, or \emph{write} (if $write$ parameter is \emph{true}), access against the specified triplestore is successfully revoked, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{issueAccessRequest(cookie, \; triplestore_{ID}, \; target,\; write)}$: \linebreak takes as input a cookie, a triplestore identifier, a username, a boolean parameter and an access token. Outputs $\textrm{SUCCESS}$ if an access change request, for the $target$ user, granting \emph{read} or \emph{write} (if the $write$ parameter is \emph{true}) permissions against the specified triplestore is sucessfully issued, $\bot$ otherwise.
        
        \item $\{[access_{req_0}, ..., access_{req_n}], \bot\} \leftarrow \mathbf{getPendingAccessRequests(cookie, \; triple{\text -}}$\linebreak $\mathbf{store_{ID}, \; at)}$: takes as input a cookie, a triplestore indentifier and an access token. Outputs the collection of the triplestore's pending access change requests, if the specified access token has sufficient permissions, $\bot$ otherwise.
        
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{processAccessRequest(cookie, \; triplestore_{ID}, \; request_{ID},}$\linebreak$\mathbf{decision, \; at)}$: takes as input a cookie, a triplestore identifier, an access change request identifier, a boolean parameter and an access token. Outputs $\textrm{SUCCESS}$ if the specified request is successfully processed according to the provided $decision$ parameter, $\bot$ otherwise.
        
        \item $\{token_{ID}, \bot\} \leftarrow \mathbf{createAccessToken(cookie, \; triplestore_{ID}, \; target)}$: takes as input a cookie, a triplestore identifier, and a username. Outputs the newly generated token identifier, if the $target$ user has sufficient permissions against the specified triplestore, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{deleteAccessToken(cookie, \; triplestore_{ID}, \; at)}$: takes as input a cookie, a triplestore identifier, and an access token. Outputs $\textrm{SUCCESS}$ if the token is successfully deleted, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{checkReadAccess(triplestore_{ID}, \; at)}$: takes as input a triplestore identifier and an access token. Outputs $\textrm{SUCCESS}$ if the token grants \emph{read} access to the specified triplestore, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{checkWriteAccess(triplestore_{ID}, \; at)}$: takes as input a triplestore identifier and an access token. Outputs $\textrm{SUCCESS}$ if the token grants \emph{write} access to the specified triplestore, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{checkOwnerAccess(triplestore_{ID}, \; at)}$: takes as input a triplestore identifier and an access token. Outputs $\textrm{SUCCESS}$ if the token grants \emph{owner} access to the specified triplestore, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{checkIfActive(at)}$: takes as input a triplestore identifier and an access token. Outputs $\textrm{SUCCESS}$ if the token is active (the associated session or the oken has not expired), $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{acquireTriplestoreLock(cookie, \; at)}$: takes as input a cookie and an access token. Outputs $\textrm{SUCCESS}$ if the token is successfully associated with a newly generated lock against the triplestore affected by the token, $\bot$ otherwise.
        
        \item $\{\textrm{SUCCESS}, \bot\} \leftarrow \mathbf{releaseTriplestoreLock(cookie, \; at)}$: takes as input a cookie and an access token. Outputs $\textrm{SUCCESS}$ if the lock associated with the token is successfully unlocked, $\bot$ otherwise.

    \end{itemize}
    \item CLIENT is a tuple $()$ of 25 polynomial time algorithms. These algorithms are responsible for ...:
    \begin{itemize}
        \item 
    \end{itemize}
    \item VAULT is a tuple $()$ of 3 polynomial time algorithms. These algorithms are responsible for ...:
    \begin{itemize}
        \item
    \end{itemize}
    \item TRIPLESTORE is a tuple $()$ of 11 polynomial time algorithms. These algorithms are responsible for ...:
    \begin{itemize}
        \item
    \end{itemize}
    \item PROXY is a tuple $()$ of 2 polynomial time algorithms. These algorithms are responsible for ...:
    \begin{itemize}
        \item $\{search_{ID}, \bot\} \leftarrow \mathbf{prepareSearch(E(nodes), \; at)}$
        \item $\{search_{ID}, \bot\} \leftarrow \mathbf{answerSPARQLQuery(nodes, \; at)}$
    \end{itemize}
\end{itemize}
\end{definition}

\subsection{Enforcing access control}

The \emph{\gls{IAM} Provider} component is responsible for enforcing access control. This functionality is achieved using the combination of algorithms scoped to the IAM protocol, specified in Definition \ref{ch3:definition:sentri}. The component's pseudocode is available in appendix \mytodo{Add \gls{IAM} Provider pseudocode as appendix}.

To prove the correctness of \gls{SEnTri}'s access control, we will prove the correctness of the enforcement of authentication, permissions and lock-based access to resources.

These procedures can be reduced to the evaluation of logical checks against the state of resources saved in the \emph{\gls{IAM} Provider} component. Such logical checks can be expressed using first-order logic.

In Chapter \ref{cha:experimental-evaluation} we will prove that the pseudocode upholds the formulas from the definitions \ref{ch3:definition:authentication}, \ref{ch3:definition:permissions} and \ref{ch3:definition:concurrent-access}, consequently proving correctness of the proposed access control solution.


\subsubsection{Authentication} 
Authentication is the process where we verify the identity of a user. We do so, by checking that the provided credentials, during login, match the ones saved within our system. To avoid having to verify this condition in every request sent by the user, we use \emph{sessions}. The session is generated upon a successful login request. A valid session will prove the identity of the user sending the requests. In \gls{SEnTri}, an authenticated user needs to be valid (has been registered within the system) and hold an active session.

\begin{definition}[\textbf{Authentication Correctness}]\label{ch3:definition:authentication} Let $IsAuth(user)$ be a predicate that evaluates to $true$ if a $user$ is authenticated, $\bot$ otherwise. Given $s_i = (session_{ID},\; username_s,\; ttl)$ and $user_u = (username_u, \; password_u,\; role_u,\; owned_u)$, correctness of the authentication procedure is upheld by the following formula:

\begin{equation}
   IsAuth(user_u) \Leftrightarrow \exists_{s} (username_u = username_s \; \wedge \; ttl > 0)
\end{equation}
\end{definition}

\subsubsection{Permissions}

Permissions in \gls{SEnTri} are logical rules which dictate whether a user is allowed to execute a certain operation, or not. 


\begin{definition}[\textbf{Permission Enforcement Correctness}]\label{ch3:definition:permissions}
Let $HasAccess(user, \; triplestore, \allowbreak type)$ be a predicate that evaluates to true if  $user$ has access of a certain $type$, to the specified $triplestore$, $\bot$ otherwise. Let $CanGrantAccess(user, \; type, \;  triplestore, \; target)$, and $CanRevokeAccess(user, \; type, \; triplestore, \; target)$ be predicates that evaluate to $true$, respectively, if a $user$ can grant or revoke access, of a certain $type$, to the specified $triplestore$, to the specified $target$ user, $\bot$ otherwise.

Let $CanCreateTriplestore(user, \; triplestore)$, $CanDeleteTriplestore(user, triplestore)$, $CanChangeOwnership(user, \; triplestore, \; target)$ be predicates that evaluate to $true$, respectively, if a $user$ can create, delete, or transfer ownership, to $target$ user, of the specified $triplestore$, $\bot$ otherwise.

Finally, let $CanGrantRole(user, \; role, \; target)$, $CanRevokeRole(user, \; role, \; target)$ be predicates that evaluate to $true$, respectively, if a $user$ can grant, or revoke, the specified $target$ user's $role$, $\bot$ otherwise. 

Given $type \in \{\textrm{READ},\textrm{WRITE},\textrm{OWNER}\}$, triplestore $T_i = (triplestore_{ID_i},\; nodes_i, \; acp_i = (r_i, w_i))$, $user_u = (username_u, \; password_u, \; role_u, \; owned_u)$, and $user_t = (username_t, \; role_t, \allowbreak password_t, \; owned_t)$ correctness of the permission evaluation procedure is upheld by the following formulas:

\begin{align}
    \begin{split} 
        Can&GrantAccess(user_u, \; type, \; T_i, \; user_t) \Leftrightarrow IsAuth(user_u) \; \wedge \; \exists_{user_t} \; user_u \neq user_t \\ 
        & \wedge \; (role_u = \textrm{ADMIN} \; \vee \; triplestore_{ID_i} \in \; owned_u ) \\
        & \\
        Can&RevokeAccess(user_u, \; type, \; T_i, \; user_t) \Leftrightarrow IsAuth(user_u) \\ 
        & \wedge \; \exists_{user_t} \; user_u \neq user_t \; \wedge \; (role_u = \textrm{ADMIN} \; \vee \; triplestore_{ID_i} \in \; owned_u ) \\
        & \\
        Has&Access(user_u, \; type, \; T_i) \Leftrightarrow role_u = \textrm{ADMIN} \; \vee \; triplestore_{ID_i} \in \; owned_u  \\ 
        & \vee \; (type = \textrm{READ} \; \wedge \; username_u \in r_i) \; \vee \; (type = \{\textrm{READ}, \; \textrm{WRITE}\} \; \wedge \; username_u \in w_i) \\
        & \vee \; (type = \textrm{OWNER} \; \wedge \; triplestore_{ID_i} \in \; owned_u) \\
        & \\
        Can&CreateTriplestore(user_u, \; T_i) \Leftrightarrow IsAuth(user_u) \\ 
        & \wedge \; role_u \in \{\textrm{ADMIN}, \; \textrm{PRIVILEGED}\} \; \wedge \; \nexists\;_{triplestore} \;\; triplestore_{ID} = triplestore_{ID_i} \\
        & \\
        Can&DeleteTriplestore(user_u, \; T_i) \Leftrightarrow IsAuth(user_u) \\ 
        & \wedge \; role_u = \textrm{ADMIN} \; \wedge \; triplestore_{ID_i} \in \; owned_u \\
        & \\
        Can&ChangeOwnership(user_u, \; T_i, \; user_t) \Leftrightarrow IsAuth(user_u) \; \wedge \; \exists_{user_t} \; user_u \neq user_t \\
        & \wedge \; (role_u = \textrm{ADMIN} \; \vee \; triplestore_{ID_i} \in \; owned_u ) \; \wedge \; role_t \neq \textrm{BASIC} \\
        & \\
        Can&GrantRole(user_u, \; role, \; user_t) \Leftrightarrow IsAuth(user_u) \\
        & \wedge \; role \neq role_t \; \wedge \; ((\exists_{user_t} \; user_u \neq user_t \; \wedge \; role_u = \textrm{ADMIN}) \\
        & \vee \; (user_u = user_t \; \wedge \; (role_u = \textrm{ADMIN} \; \vee \; role = \textrm{BASIC}))) \\
        & \\
        Can&RevokeRole(user_u, \; role, \; user_t) \Leftrightarrow IsAuth(user_u)\\
        & \wedge \; role \neq role_t \; \wedge \; ((\exists_{user_t} \; user_u \neq user_t \; \wedge role_u = \textrm{ADMIN}) \; \vee \; user_u = user_t) 
    \end{split}
\end{align}

\end{definition}


\subsubsection{Concurrent Access} 

Concurrent operations can target users or triplestores. The correctness of concurrent write and read operations is achieved using a \emph{lock-based} approach. Resources (users and triplestores) can be in a \emph{locked} or \emph{unlocked} state. Write operations can only be requested against \emph{unlocked} resources, which remain locked while the operation is being executed. Consequently, read operations can occur simultaneously, but write operations are sequential, against the same resource.

\emph{Locks} are enough for requests targeting users. Operations can only be executed by users, against themselves, or by \emph{admin} users, and the origin of the requests is always the \emph{Client} (the execution context is constant).

Operations against triplestores are more complex. The query evaluation pipeline starts in the \emph{Client} component, but subsequent steps are executed in the \emph{Triplestore}, \emph{Vault} and \emph{Secure Computation Proxy}. Furthermore, whenever the execution context changes, we must verify if the author of the request has sufficient permissions.

To fulfill such requirements, every request from the \emph{Client} to other components, contains an \emph{access token}. The other components execute permission checks, by sending this token to the \emph{\gls{IAM} Provider}. The \emph{\gls{IAM} Provider} verifies the validity of the token, and if its associated permissions are correct. 

Queries that contain write predicates, will require that their target remains \emph{locked}, during their evaluation. As such, an \emph{access token} can be associated with a \emph{lock}. The triplestore will remain in a \emph{locked} state until the \emph{access token} is deleted or the \emph{lock} expires. 

\begin{definition}[\textbf{Concurrent Lock-based Access Correctness}]\label{ch3:definition:concurrent-access}
    Let $IsLocked(resource, \; l)$ and $CanLock(triplestore, \; at)$ be predicates that evaluate to $true$ if, respectively, $resource$ is in a $locked$ state associated with the lock $l$ and $triplestore$ can transition to a $locked$ state, and associate the generated lock with access token $at$, $\bot$ otherwise. 
    
    Let $CanExecuteOperations(triplestore, \; at, \; type)$ be a predicate that evaluates to $true$ if the access token $at$ grants sufficient permissions, against the target $triplestore$, to allow operations, of the specified access $type$, to be executed in the current context of execution, $\bot$ otherwise.

    Given $type \in \{\textrm{READ},\textrm{WRITE},\textrm{OWNER}\}$, triplestore $T_i = (triplestore_{ID_i},\; nodes_i, \;  acp_i = (r_i, w_i))$, lock $l_a = (lock_{ID_a},\; resource_{ID_a},\; ttl_a)$, lock $l_b = (lock_{ID_b},\; resource_{ID_b},\; ttl_b)$, access token $at_k = (token_{ID_k}, \; username_k,\; ttl_k, \; session_{ID_k}, \; triplestore_{ID_k}, \; lock_{ID_k})$, user $user_u = (username_u, \; password_u, \; role_u, \; owned_u)$ and session $s_j = (session_{ID_j}, \; username_j, \; ttl_j)$, let the resources $resource_a$ and $resource_b$ be identified by, respectively, $resource_{ID_a}$ and $resource_{ID_b}$. The correctness of concurrent access is upheld by the following formulas:

    \begin{align}
        \begin{split} 
           IsL&ocked(resource_a, \; l_a) \Rightarrow \exists_{l_a}\exists_{resource_a}(ttl_a \; > \; 0) \\
           & \wedge \; \nexists_{l_b}(resource_{ID_b} \; = \; resource_{ID_a} \; \wedge \; lock_{ID_a} \neq \; lock_{ID_b} \; \wedge \; ttl_b \; > \; 0) \\
           & \wedge \; \nexists_{l_b}(resource_{ID_b} \; \neq \; resource_{ID_a} \; \wedge \; lock_{ID_a} = \; lock_{ID_b} \; \wedge \; ttl_b \; > \; 0) \\
            & \\
            Ca&nLock(T_i, \; at_k) \Leftrightarrow \nexists_l(IsLocked(T_i, \; l)) \; \wedge \; triplestore_{ID_i} \; = \; triplestore_{ID_k} \\
            & \wedge \; username_k \; = \; username_u \; \wedge \; username_j \; = \; username_u \; \wedge \; ttl_j \; > \; 0 \\
            & \wedge \; ttl_k \; > \; 0 \; \wedge \; HasAccess(user_u, \; type, \; T_i) \; \wedge \; type \in \{\textrm{WRITE}, \; \textrm{OWNER}\} \\
            & \\
            Ca&nExecuteOperations(T_i, \; at_k, \; type) \Leftrightarrow triplestore_{ID_i} \; = \; triplestore_{ID_k} \\
            & \wedge \; username_k \; = \; username_u \; \wedge \; username_j \; = \; username_u \; \wedge \; ttl_j \; > \; 0 \\
            & \wedge \; ttl_k \; > \; 0 \; \wedge \; HasAccess(user_u, \; type, \; T_i) \; \wedge \; ( type \; = \; \textrm{READ} \\
            & \vee \; (type \in \{\textrm{WRITE}, \; \textrm{OWNER}\} \; \wedge \; IsLocked(T_i, \; l_a) \; \wedge \; lock_{ID_a} \; = \; lock_{ID_k}))
        \end{split}
    \end{align}  
\end{definition}

\subsection{Secret sharing}
\subsection{Triple pattern encryption schemes}
\subsubsection{Base encryption schemes}

\lstinputlisting[caption={Base encryption scheme}, label={encryption-scheme-base}]{Code/chapter3/scheme-base.tex}

\lstinputlisting[caption={Triple Pattern Encryption for Protocol I}, label={encryption-scheme-deterministic}]{Code/chapter3/scheme-deterministic.tex}

\lstinputlisting[caption={Triple Pattern Encryption for Protocol II}, label={encryption-scheme-dgk}]{Code/chapter3/scheme-dgk.tex}

\subsection{Evaluation of SPARQL queries}
\paragraph{Query Plan Generation}
\paragraph{Query Rewriting}
\paragraph{Query Preparation Phase}
\paragraph{Query Evaluation Phase}




