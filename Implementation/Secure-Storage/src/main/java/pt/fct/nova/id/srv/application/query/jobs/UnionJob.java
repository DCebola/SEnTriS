package pt.fct.nova.id.srv.application.query.jobs;

public class UnionJob extends Job{
    public UnionJob(String jobID, VariablesPattern variablesPattern) {
        super(variablesPattern, jobID);
    }
}
